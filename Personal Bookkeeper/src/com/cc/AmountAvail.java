/*package com.cc;

public class AmountAvail extends AccountNumber
{
    private double amount;
    private double modAmount;

    //constructors
    public AmountAvail(int acNum, double amount, double modAmount)
    {
        super(acNum);
        getAmount(amount);
        newAmount(modAmount);
    }

    //method getAmount
    public double getAmount(double amount) { return this.amount; }

    //method newAmount
    public double newAmount(double modAmount)
    {
        this.modAmount = amount - modAmount;
    }
}
*/